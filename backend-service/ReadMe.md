# Getting started with Kong Flip Tool

## Pre-requisites:
 * nodejs 

# Local Environment Setup
* Install nodejs

* Clone the Project and install the required node modules

    ```
    cd KongEnvTool
    npm install
    ```

* Run the service
    ```
    npm start
    ```
